{
  "name": "imq-rpc-exampple-client",
  "version": "0.0.1",
  "description": "IMQ-PRC service example service",
  "keywords": [
    "imq-rpc"
  ],
  "scripts": {
    "build": "tsc",
    "static": "npm run generate && npm run build && node static.js",
    "runtime": "npm run build && node runtime.js",
    "generate": "imq client generate Hello -o"
  },
  "author": "Serhii Fesiura <peinguin@gmail.com>",
  "homepage": "https://imqueue.com/",
  "license": "ISC",
  "dependencies": {
    "@imqueue/cli": "^1.0.2",
    "@imqueue/rpc": "^1.2.1",
    "@types/node": "^10.3.3",
    "typescript": "^2.9.2"
  },
  "devDependencies": {},
  "typescript": {
    "definitions": "index.d.ts"
  }
}
